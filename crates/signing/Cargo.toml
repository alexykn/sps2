[package]
name = "sps2-signing"
version = "0.1.0"
edition = "2021"

[lib]
name = "sps2_signing"
path = "src/lib.rs"

[dependencies]
minisign-verify = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sps2-errors = { path = "../errors" }

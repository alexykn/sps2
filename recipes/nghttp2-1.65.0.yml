metadata:
  name: nghttp2
  version: "1.65.0"
  description: "HTTP/2 C library and tools"
  license: "MIT"

environment:
  defaults: true

source:
  fetch:
    url: "https://github.com/nghttp2/nghttp2/releases/download/v1.65.0/nghttp2-1.65.0.tar.bz2"

build:
  system: autotools